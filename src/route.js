import React from 'react';
import {Switch, Route} from 'react-router-dom';

export default (
    <Switch>
        <Route exact path='/'/>
        <Route exact path='/landing'/>
        <Route exact path='/accountable'/>
        <Route exact path='/skillshare'/>
    </Switch>
)